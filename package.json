{
  "name": "metroscope-flow",
  "private": true,
  "scripts": {
    "dev": "node dev.mjs",
    "build": "npm run build --prefix frontend",
    "test": "npm run test --prefix backend"
  },
  "dependencies": {
    "mongoose": "^9.8.0"
  }
}
